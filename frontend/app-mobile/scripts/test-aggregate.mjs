import { aggregateIngredients } from '../services/shoppingList.mjs';

// This script is intended to be run in Node if you transpile or adjust imports.
console.log('Run tests inside the app or verify via UI.');